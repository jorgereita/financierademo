// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  baseUrl : 'https://desarrollo.epik.com.co:5001', // dev

  // baseUrl : 'https://diunsa.epik.com.co:6645/', // prod
  // baseUrl : 'http://localhost:52103/',
  baseNode: 'https://desarrollo.epik.com.co:6601',
  // baseNode: 'http://localhost:8080',
  nombreProyecto: 'Diunsa Epik (Pruebas)'
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
